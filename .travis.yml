jobs:
  include:
    - os: linux
      dist: xenial
      python: 3.4
    - os: linux
      dist: xenial
      python: 3.5
    - os: linux
      dist: xenial
      python: 3.6
    - os: linux
      dist: xenial
      python: 3.7
    - os: osx
      python: 3.6
language: python
sudo: false
install:
  - pip install -r requirements-test.txt
  - pip install flake8
  - python setup.py install
script:
    - pytest
    - flake8
